services:
  frontend:
    build:
      context: ./frontend
    ports:
      - "3000:3000"

  backend:
    build:
      context: ./backend
    ports:
      - "8080:8080"

  # db:
  #   image: postgres
  #   restart: always
  #   shm_size: 128mb
  #   environment:
  #     POSTGRES_USER: admin
  #     POSTGRES_PASSWORD: 1234

  # adminer:
  #   image: adminer
  #   restart: always
  #   ports:
  #     - 8081:8080
